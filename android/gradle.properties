org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# flutter_secure_storage requires compileSdk 37, which is above AGP 9.1.0's
# maximum recommended value of 36. The combination builds correctly; this
# suppresses the advisory failure. Remove once AGP officially supports 37.
android.suppressUnsupportedCompileSdk=37
